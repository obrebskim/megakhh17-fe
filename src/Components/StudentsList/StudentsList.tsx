import React from "react";
import styled from "styled-components";

const Container = styled.section``;

function StudentsList() {
  return <Container>dostępni kursanci</Container>;
}

export default StudentsList;
